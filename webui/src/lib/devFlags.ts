/**
 * Dev-only UI flags. Flip these off (or gate on import.meta.env.DEV) before a
 * stage/production build — nothing here is required for correct operation.
 */

/** Show "JUCE" / "WS" next to the header connection dot. */
export const SHOW_TRANSPORT_LABEL = true;
