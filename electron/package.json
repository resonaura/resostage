{
  "name": "resostage-electron",
  "private": true,
  "version": "0.1.0",
  "description": "Electron shell for the ResoStage web UI (Settings > UI = Electron)",
  "type": "module",
  "main": "dist/main.mjs",
  "scripts": {
    "build": "tsc && node scripts/build/native/omni.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "brand": "node scripts/brand-mac-app.mjs",
    "start": "pnpm build && electron ."
  },
  "dependencies": {
    "koffi": "^2.10.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^37.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
