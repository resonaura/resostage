import { Checkbox as HeroCheckbox } from "@heroui/react";

/**
 * Design-system entry point for HeroUI's accessible compound checkbox.
 * Keeping the export here prevents feature screens from bypassing shared UI
 * policy when checkbox styling or behaviour changes later.
 */
export const Checkbox = HeroCheckbox;
